# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'InterwareFacebook' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for InterwareFacebook
  pod 'Parse'
  pod 'Alamofire'
  pod 'Google/Analytics'
  pod 'CryptoSwift'

  target 'InterwareFacebookTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InterwareFacebookUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
